# Local development backend - 같은 네트워크의 다른 기기에서도 접속 가능
# PC에서 접속할 때도 이 주소 사용 가능
# 폰이나 다른 기기에서 접속할 때 이 IP 사용
VITE_API_URL=http://localhost:8002

# For localhost only (PC에서만):
# VITE_API_URL=http://localhost:8001

# For production GPU server:
# VITE_API_URL=http://your-gpu-server-ip:8001